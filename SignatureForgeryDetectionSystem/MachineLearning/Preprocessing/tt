from sklearn.metrics import accuracy_score, precision_score, recall_score, f1_score, confusion_matrix

# Get model predictions on validation data
y_true = []
y_pred = []

for (img_batch, feature_batch), labels in val_generator:
    predictions = model.predict([img_batch, feature_batch])
    y_true.extend(labels)
    y_pred.extend((predictions > 0.5).astype(int).flatten())  # Convert probabilities to binary labels

# Convert lists to numpy arrays
y_true = np.array(y_true)
y_pred = np.array(y_pred)

# Compute metrics
accuracy = accuracy_score(y_true, y_pred)
precision = precision_score(y_true, y_pred)
recall = recall_score(y_true, y_pred)
f1 = f1_score(y_true, y_pred)
conf_matrix = confusion_matrix(y_true, y_pred)

# Print results
print(f"Accuracy: {accuracy:.4f}")
print(f"Precision: {precision:.4f}")
print(f"Recall: {recall:.4f}")
print(f"F1-score: {f1:.4f}")
print("Confusion Matrix:")
print(conf_matrix)
